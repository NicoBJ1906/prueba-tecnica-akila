# Secretos — nunca comitear
.env
.env.*
!.env.example
*.key
secrets.toml
.streamlit/secrets.toml

# Entornos virtuales
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Salida del triaje: se regenera en cada ejecución y no debe versionarse.
# Si se versionara, quien clone el repositorio ejecutaría el proceso sobre un
# Excel que ya tiene filas y acabaría con el doble. La ejecución de referencia
# está guardada aparte, en docs/ejemplo-ejecucion/.
ejercicio1_triaje/salida/

# Sistema
.DS_Store

# Notas internas de trabajo, no forman parte de la entrega: la libreta entre
# sesiones y el guion para defender la presentación. Se quedan en local.
ESTADO.md
docs/presentacion.md
